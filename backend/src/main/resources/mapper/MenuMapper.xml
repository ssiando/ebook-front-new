<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mict.ebook.menu.repository.mapper.MenuMapper">

    <resultMap id="menuResultMap" type="com.mict.ebook.menu.domain.Menu">
        <id property="id" column="id" />
        <result property="parentId" column="parent_id" />
        <result property="label" column="label" />
        <result property="path" column="path" />
        <result property="sortOrder" column="sort_order" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>

    <select id="findAllOrderBySortOrder" resultMap="menuResultMap">
        SELECT id, parent_id, label, path, sort_order, created_at, updated_at
        FROM menu
        ORDER BY sort_order ASC, id ASC
    </select>

    <sql id="searchCondition">
        <where>
            <if test="parentId != null">
                AND m.parent_id = #{parentId}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (m.label LIKE CONCAT('%', #{keyword}, '%')
                OR m.path LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </sql>

    <select id="search" resultMap="menuResultMap">
        SELECT m.id, m.parent_id, m.label, m.path, m.sort_order, m.created_at, m.updated_at
        FROM menu m
        <include refid="searchCondition" />
        ORDER BY m.sort_order ASC, m.id ASC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countBySearch" resultType="long">
        SELECT COUNT(*)
        FROM menu m
        <include refid="searchCondition" />
    </select>

    <select id="findById" resultMap="menuResultMap">
        SELECT id, parent_id, label, path, sort_order, created_at, updated_at
        FROM menu
        WHERE id = #{id}
    </select>

    <select id="existsById" resultType="boolean">
        SELECT EXISTS(
        SELECT 1 FROM menu
        WHERE id = #{id}
        )
    </select>

    <insert id="insert" parameterType="com.mict.ebook.menu.domain.Menu">
        INSERT INTO menu (id, parent_id, label, path, sort_order)
        VALUES (#{id},
        #{parentId},
        #{label},
        #{path},
        #{sortOrder})
    </insert>

    <update id="update" parameterType="com.mict.ebook.menu.domain.Menu">
        UPDATE menu
        SET parent_id  = #{parentId},
            label      = #{label},
            path       = #{path},
            sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE
        FROM menu
        WHERE id = #{id}
    </delete>

    <select id="countChildrenByParentId" resultType="long">
        SELECT COUNT(*)
        FROM menu
        WHERE parent_id = #{parentId}
    </select>

</mapper>
